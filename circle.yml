# encoding: UTF-8
---
machine:
  services:
    - docker

checkout:
  post:
    - git submodule sync
    - git submodule update --init

dependencies:
  override:
    - docker info
    - make build

test:
  override:
    - make test

deployment:
  hub:
    branch: master
    commands:
      - make push